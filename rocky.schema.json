{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/minjun0219/rocky/main/rocky.schema.json",
  "title": "rocky config",
  "description": "Configuration for rocky — `rocky.json`. Project config (./rocky.json) overrides user config (~/.config/rocky/rocky.json) field by field. The `worklog` block is read by the TypeScript worklog server; the `todo` block is read by the Rust daemon.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Reference to this JSON Schema. Editors use this for autocomplete and validation."
    },
    "worklog": {
      "type": "object",
      "description": "Settings for the worklog_* tools (append-only agent journal), the Stop hook auto-capture, and the /rocky:recall digest.",
      "properties": {
        "dir": {
          "type": "string",
          "minLength": 1,
          "description": "worklog JSONL 저장 디렉터리. Defaults to a per-project path under ~/.config/rocky/worklog/<project-key>. ROCKY_WORKLOG_DIR wins."
        },
        "autoCapture": {
          "type": "boolean",
          "description": "Stop hook 자동 기록 on/off (기본 true). env ROCKY_WORKLOG_AUTO_CAPTURE 가 우선."
        },
        "captureMaxChars": {
          "type": "integer",
          "minimum": 1,
          "description": "turn req/did 최대 글자 (기본 800)"
        },
        "digestThreshold": {
          "type": "integer",
          "minimum": 1,
          "description": "/rocky:recall Haiku↔Sonnet 임계 (기본 40)"
        }
      },
      "additionalProperties": false
    },
    "todo": {
      "type": "object",
      "description": "Board daemon settings, read by the Rust daemon (port / dir / expose / watch / statusline). The TypeScript loader passes this block through without validating it; the daemon owns the shape.",
      "additionalProperties": true
    }
  }
}
