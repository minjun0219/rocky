{
  "$schema": "https://json.schemastore.org/claude-code-marketplace.json",
  "name": "rocky-dev",
  "owner": {
    "name": "Minjun Kim"
  },
  "description": "Local dev marketplace — serves the rocky plugin in place from this working tree (directory source \"./\") so /reload-plugins picks up edits without push/clone. Add with: claude plugin marketplace add ./marketplace.dev.json",
  "plugins": [
    {
      "name": "rocky",
      "source": "./",
      "description": "rocky plugin read in place from the local working tree (dev loop). Same surface as rocky@rocky-marketplace."
    }
  ]
}
