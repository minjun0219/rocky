node_modules/
dist/
*.log
.DS_Store
.agent-cache/
.sisyphus/
.env
.env.local
# .env.production / .env.development 등 나머지 변형
.env.*
*.tsbuildinfo
# bun build 가 outfile 미지정 시 만드는 sibling artifacts (예: bin/openapi-mcp-<hash>)
bin/openapi-mcp-*
# 로컬 전용 캐시/워크로그 (ROCKY_WORKLOG_DIR / ROCKY_NOTION_CACHE_DIR 가 in-tree 로 향할 때 대비)
*.jsonl
notion-pages/
# 로컬 프로젝트 설정 (private registry URL 등을 담을 수 있음). rocky.schema.json 은 추적 유지.
rocky.json
# Claude Code 로컬 오버라이드 (권한/시크릿 보관 위치)
.claude/settings.local.json
# 외부 에이전트 하네스가 레포 안에 남기는 로컬 산출물 (추적 대상 아님)
.superpowers/
.opencode/
# 구 monorepo 잔재가 다시 생기지 않도록
packages/

