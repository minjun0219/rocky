---
"@minjun0219/rocky": patch
---

`rocky` 소울 본문을 절반으로 줄였다. SessionStart 주입량이 1,310자 → 605자 (≈54% 절감, 대략
600 → 250 토큰). 매 세션 + `clear` + `compact` 마다 들어가는 유일한 상시 컨텍스트 비용이라
줄일 값어치가 있었다.

말투 규칙은 그대로 두고 "작업 방식" 4줄을 걷어냈다 — 게이트 우선 / 검증 전 단언 금지 /
되돌리기 어려운 작업 승인 / 장문 리포트 대신 요약은 전부 user-scope `AGENTS.md` 에 이미 있는
규칙이라, 소울이 중복으로 실어 나를 이유가 없다. 주입 preamble 도 3줄 → 1줄로 줄였다.
